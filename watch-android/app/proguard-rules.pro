# R8 / ProGuard rules for the Wear OS companion app.
#
# Intentionally empty. AndroidX, Compose for Wear OS and Play Services Wearable
# all publish their own consumer rules, so the release build needs nothing extra
# for the current (placeholder) code.
#
# Add rules here only when a release build actually breaks — and say which class
# and which library needed it, so the rule can be removed when the library fixes
# its own consumer rules.
