# Fastlane Setup

- `Fastfile`: lanes for iOS/Android build
- `Appfile`: app identifiers
- `Matchfile`: code signing

## Build lanes

- `fastlane ios build_dev`: iOS dev build
- `fastlane android build_dev`: Android dev build

## Code signing

- Configure secrets and certificates as per project requirements
